http三次握手
客户端发送一个"我要创建一个连接"的数据包请求,发送到服务端(SYN/SEQ=X)
服务端接收到数据包后会开启TCP端口 返回给客户端标志位(SYN/ACK=X+1/Seq=Y+1)
客服端再去发送ACK=Y+1/SEQ=Z
防止服务端开启无用链接 所以需要三次握手
没有第三次握手服务端根本不知道客户端有没有创建

http不是连接 而是请求响应
如果TCP请求保持 那么第二次请求就没有三次握手的开销
